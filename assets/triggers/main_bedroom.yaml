## TRIGGERS / MAIN BEDROOM

# keep occupancy detection around for a bit
- trigger:
    - platform: state
      entity_id: binary_sensor.main_bedroom_prescence_sensor_presence
      to: 'on'
  binary_sensor:
    name: 'Main Bedroom Occupancy Timeout'
    state: "on"
    auto_off:
      minutes: 5
