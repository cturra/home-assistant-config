## KITCHEN

- sensor:
    # -- sensor push --
    # fridge temperature
    - name: 'Fridge Temperature 1 Decimal'
      unique_id: fridge_temperature_1_decimal
      device_class: temperature
      unit_of_measurement: '°C'
      state: > 
        {{ states('sensor.fridge_temperature') | round(1) }}