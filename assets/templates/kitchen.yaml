## KITCHEN

- sensor:
    # -- sensor push --
    # fridge temperature
    - name: 'Fridge Temperature 1 Decimal'
      device_class: temperature
      unit_of_measurement: '°C'
      state: > 
        {{ states('sensor.fridge_temperature') | round(1, default=1.0) }}