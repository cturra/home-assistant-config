## CRAWLSPACE

- sensor:
    # -- xiaomi aqara --
    # temperature
    - name: 'Crawlspace Temperature 1 Decimal'
      unique_id: crawlspace_temperature_1_decimal
      device_class: temperature
      unit_of_measurement: '°C'
      state: >
        {{ state_attr('sensor.snr_crawlspaceclimate_temperature', 'temperature') | round(1) }}

    # -- sensor push --
    # (deep) freezer temperature
    - name: 'Freezer Temperature 1 Decimal'
      unique_id: freezer_temperature_1_decimal
      device_class: temperature
      unit_of_measurement: '°C'
      state: > 
        {{ states('sensor.freezer_temperature') | round(1) }}