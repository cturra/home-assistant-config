## TEMPLATES / SPARE BEDROOM

# --- sensors ---
- sensor:
    # -- xiaomi aqara --
    # temperature
    - name: 'Spare Bedroom Temperature 1 Decimal'
      device_class: temperature
      unit_of_measurement: '°C'
      state: >
        {{ state_attr('sensor.snr_sparebedroom_climate_temperature', 'temperature') | round(1, default=20.0) }}